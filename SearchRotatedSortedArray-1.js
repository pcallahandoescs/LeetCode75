/**
 * @param {number[]} nums
 * @param {number} target
 * @return {number}
 */
const search = (nums, target) => {
    return nums.indexOf(target);
};

//this code runs and is by all means technically correct